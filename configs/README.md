# configs

Reserved for saved experiment presets and benchmark settings.

The current minimal scaffold exposes every runtime parameter directly through the CLI, so this directory is only a placeholder for future reproducible experiment configs.
